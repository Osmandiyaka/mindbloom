# MindBloom Branding Tasks

- [x] Create brand token file with teal-leaning palette for light/dark.
- [x] Populate semantic tokens (surface, text, border, focus, primary states).
- [x] Wire brand tokens into global styles and set default theme to MindBloom Light.
- [x] Add official MindBloom logo SVG assets (icon + lockups).
- [x] Add reusable `MbLogoComponent` with variants and sizes.
- [x] Apply `MbLogoComponent` + brand tokens to the login page.
- [x] Add minimal brand helper utilities.

Notes:
- Tokens remain CSS variables; no hex in components.
- Auth UI stays enterprise-safe: no gradients, blur, or decorative shapes.
