export enum AssignBehavior {
    IMMEDIATE = 'immediate',
    SCHEDULED = 'scheduled',
    KEEP_SUSPENSION = 'keep_suspension',
    EXPIRED_ASSIGNMENT = 'expired_assignment',
}

export enum ProrationPolicy {
    IMMEDIATE_PRORATE = 'immediate_prorate',
    END_OF_CYCLE = 'end_of_cycle',
    NO_PRORATION = 'no_proration',
}
