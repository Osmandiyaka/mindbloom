# Student Detail Drawer Tasks

1) Drawer shell and responsive behavior (right drawer desktop, bottom sheet mobile).
2) Sticky header with actions and status metadata.
3) Summary strip (enrollment, guardian, fees/attendance, last updated).
4) Tabs scaffold with URL sync + permission/entitlement gating.
5) Overview tab (info grid, enrollment summary, resolve flags).
6) Guardians tab (table, actions, add guardian modal).
7) Academics tab (subjects + term history + report action).
8) Fees tab (summary + invoices + payments).
9) Notes tab (notes feed + create/edit/delete).
10) Documents tab (documents table + upload/verify).
11) Activity/Audit tab (log table + filters + detail view).
12) Destructive confirmations + impact preview for archive/transfer/merge.
13) Performance + UX polish (skeletons, cancellation, keyboard, copy).
14) API wiring for all tabs (no hardcoding, per-tab errors).
